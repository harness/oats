{
  "name": "@harnessio/oats-examples",
  "version": "0.0.1",
  "license": "MIT",
  "type": "module",
  "private": "true",
  "scripts": {
    "build": "oats import --clean",
    "postbuild": "exit 0 && tsc --noEmit",
    "lint": "eslint 'output/**/*.{mts,ts}'",
    "fmtc": "prettier --check 'output/**/*.{mts,ts}'",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "@harnessio/oats-cli": "workspace:^",
    "@harnessio/oats-plugin-react-query": "workspace:^",
    "@tanstack/react-query": "^4.20.4",
    "react": "^18.2.0",
    "typescript": "^4.8.4"
  }
}
